# legacy — the 2022 prototype

Original class-project code, frozen for provenance: Safari + `mss` screenshots at
1 Hz, `pylsd` line detection as enemy perception, and OS-mouse clicks for steering.
Superseded by the `slither_bot/` package at the repo root; kept unchanged
(bugs and all) as part of the project history.
